# 更新日志

本项目语义化版本见 `metadata.yaml`。本文件记录各版本的显著行为变更。

## [2.0.2] - 2026-09-01

### 修复：超长组合标记静默丢图 + WebUI 一批缺陷

**修复**：组合标记超过 6 个 token 时，第 6 位之后可命中的 tag 不再被按位置截断丢弃。组合枚举仍限前 6 个 token（防子集数指数膨胀），但超出部分逐个做单 tag 兜底探测，任何位置可命中的 tag 都能发图。

**WebUI 修复**：

- 搜索/翻页/筛选的旧响应竞态：慢网下先发的请求晚回包不再覆盖新结果。
- 删空最后一页后停在越界空页：页码现在钳回最后一页（前后端双侧）。
- 批量删除上限 100 → 500，并消费后端 `errors`，部分失败时如实提示。
- 选择模式下点选/全选/反选改为局部更新，不再整墙 48 张卡片重建。
- 编辑抽屉打开时，搜索/翻页/刷新不再清空未保存的表单输入。
- 图片 base64 缓存加上限（240 项），长会话不再无限膨胀。
- 翻页/搜索/打开详情等路径的请求失败不再静默（unhandled rejection），统一 toast 提示。
- 格式化轮询定时器不再在关闭弹窗的瞬间"僵尸重启"，顶栏徽标在弹窗关闭后也能实时更新。
- 选择模式与文档模式的工具栏显隐冲突（退出文档后误现按钮）修复。

**清理**：

- 删除零调用死代码：`render.render_text`、storage 8 个 v1 兼容方法、page_api 孤儿缩略图清理（facade 已有同名实现）、review.py 正则回退启发式残留。
- `webui_upload_max_mb` 配置补齐声明（原先有读取但 schema/模板均未声明，用户无法配置）。
- 远程 URL 判定、缩略图目录路径、base64 data-url 组装等重复实现收敛为单一 helper。

## [2.0.1] - 2026-08-03

### 修复：表情包分布严重不均

**问题**：表情包池约 300 张，但大模型只反复使用极少数几张（使用次数第一的 160+ 次，第十名不到 3 次）。

**根因**：每轮注入 prompt 的表情包索引固定取**字母序前 30 个 `meme_def`**，其余约 270 张模型永远看不到，只能反复点名索引里的熟悉名字。

**改动**：

- `storage.get_all_meme_defs`：排序由字母序改为**冷门优先**——`usage_count` 升序 → `last_used_at` 升序（从未用过的排最前）→ `meme_def` 字典序兜底。冷门表情包进入模型可见区，被用一次即让位给下一批，形成动态轮换。
- `storage.get_all_tags`：按"该 tag 下最冷门资产"排序，冷门 tag 前置。
- `facade.rough_search_memes`：搜索兜底结果同分时按 `usage_count` 升序，冷门相关表情包靠前。

**行为不变**：tag 匹配同分决胜原本就是"最久未用、最少使用优先"（`render._pick_top_scored_asset`），未改动；选择逻辑不引入随机，结果可复现。
